1. If we are given an array and asked to write an algorithm that runs in O(log n) time, it probably want binary search
2. Similar to above, if we are given a sorted array it is likely also looking for binary search
3. In math problems, remember how to use modulo and division to pick apart a number.
4. In math problems, remember how to use base 10 multiplication to reconstruct a number
5. In math problems, don't forget basic arithmetic like you see with remainder (carry)
